@{
    var activeRole = ViewBag.ActiveRole as string ?? "Admin";
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0" />

    <title>@ViewData["Title"] - BuyZaar Admin</title>

    <link rel="stylesheet"
          href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <link rel="stylesheet"
          href="~/css/admin-dashboard.css"
          asp-append-version="true" />

    @await RenderSectionAsync("Styles", required: false)
</head>

<body>
    <div class="dashboard-layout">

        <aside class="dashboard-sidebar">
            <div class="sidebar-main">

                <div class="sidebar-logo">
                    <img src="~/images/icon_logo.png"
                         alt="BuyZaar Logo" />

                    <h5>BuyZaar</h5>
                    <span>Admin Panel</span>
                </div>

                <nav class="sidebar-nav">

                    <a asp-controller="Admin"
                       asp-action="Index"
                       class="sidebar-link @(currentAction == "Index" ? "active" : "")">
                        Dashboard
                    </a>

                    <div class="sidebar-group-title">
                        User Management
                    </div>

                    <a asp-controller="Admin"
                       asp-action="Users"
                       class="sidebar-link @(currentAction == "Users" ? "active" : "")">
                        Manage Users
                    </a>

                    <a asp-controller="Admin"
                       asp-action="SellerApplications"
                       class="sidebar-link @(currentAction == "SellerApplications" ? "active" : "")">
                        Seller Applications
                    </a>

                    <div class="sidebar-group-title">
                        Product Management
                    </div>

                    <a asp-controller="Admin"
                       asp-action="Products"
                       class="sidebar-link @(currentAction == "Products" ? "active" : "")">
                        Manage Products
                    </a>

                    <div class="sidebar-group-title">
                        Order Management
                    </div>

                    <a asp-controller="Admin"
                       asp-action="Orders"
                       class="sidebar-link @(currentAction == "Orders" ? "active" : "")">
                        Manage Orders
                    </a>

                    <a asp-controller="Admin"
                       asp-action="CancellationRequests"
                       class="sidebar-link @(currentAction == "CancellationRequests" ? "active" : "")">
                        Cancellation Requests
                    </a>

                    <a asp-controller="Admin"
                       asp-action="ReturnedOrders"
                       class="sidebar-link @(currentAction == "ReturnedOrders" ? "active" : "")">
                        Returned Orders
                    </a>

                    <a asp-controller="Admin"
                       asp-action="Payments"
                       class="sidebar-link @(currentAction == "Payments" ? "active" : "")">
                        Manage Payments
                    </a>

                    <div class="sidebar-group-title">
                        Monitoring
                    </div>

                    <a asp-controller="Admin"
                       asp-action="Reports"
                       class="sidebar-link @(currentAction == "Reports" ? "active" : "")">
                        Reports & Analytics
                    </a>

                    <a asp-controller="Admin"
                       asp-action="AuditLogs"
                       class="sidebar-link @(currentAction == "AuditLogs" ? "active" : "")">
                        Audit Logs
                    </a>

                    <a asp-controller="Admin"
                       asp-action="ManageSeller"
                       class="sidebar-link @(currentAction == "ManageSeller" ? "active" : "")">
                        Manage Sellers
                    </a>

                    <a asp-controller="Admin"
                       asp-action="Riders"
                       class="sidebar-link @(currentAction == "Riders" ? "active" : "")">
                        Manage Riders
                    </a>

                </nav>
            </div>

            <div class="sidebar-account">
                <div class="sidebar-user">

                    <div class="sidebar-avatar">
                        @((User.Identity?.Name ?? "A")
                            .Substring(0, 1)
                            .ToUpper())
                    </div>

                    <div class="sidebar-user-info">
                        <div class="sidebar-user-name">
                            @User.Identity?.Name
                        </div>

                        <div class="sidebar-user-role">
                            @activeRole
                        </div>
                    </div>

                </div>

                <form asp-controller="Account"
                      asp-action="Logout"
                      method="post">

                    @Html.AntiForgeryToken()

                    <button type="submit"
                            class="sidebar-logout-btn">
                        Sign out
                    </button>

                </form>
            </div>
        </aside>

        <main class="dashboard-main">

            <div class="dashboard-topbar">
                <div>
                    <h4>@ViewData["Title"]</h4>
                    <p>Manage BuyZaar system operations</p>
                </div>

                <div class="dashboard-topbar-actions">
                    <button class="notification-btn">
                        🔔
                        <span class="notification-badge">3</span>
                    </button>
                </div>
            </div>

            <div class="dashboard-content">
                @RenderBody()
            </div>

        </main>
    </div>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>