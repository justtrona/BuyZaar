@model BuyZaar.ViewModels.ProductViewModel

@{
    Layout = "_DashboardLayout";
    ViewBag.ActiveRole = "Seller";
    ViewData["Title"] = "Add Product";
}

<link rel="stylesheet" href="~/css/product.css" />

<div class="product-page-shell">
    <div class="product-page-header">
        <div class="header-left">
            <span class="role-badge seller-badge">Seller</span>
            <h1>Add Product</h1>
            <p>Create a new product listing for your shop.</p>
        </div>

        <div class="header-right">
            <a asp-controller="Seller" asp-action="Index" class="btn btn-light back-btn">
                Back to Dashboard
            </a>
        </div>
    </div>

    <div class="product-form-card">
        <div class="form-card-header">
            <h3>Product Information</h3>
            <p>Fill in the details below to add a new item to your store.</p>
        </div>

        @if (!ViewData.ModelState.IsValid)
        {
            <div asp-validation-summary="All" class="alert alert-danger custom-alert"></div>
        }

        <form asp-action="CreateProduct" method="post" enctype="multipart/form-data" novalidate>
            @Html.AntiForgeryToken()

            <div class="product-form-grid">
                <div class="form-group full-width">
                    <label asp-for="Name" class="form-label">Product Name</label>
                    <input asp-for="Name" class="form-control custom-input" placeholder="Enter product name" />
                    <span asp-validation-for="Name" class="text-danger small"></span>
                </div>

                <div class="form-group full-width">
                    <label asp-for="Description" class="form-label">Description</label>
                    <textarea asp-for="Description" class="form-control custom-input custom-textarea" rows="5" placeholder="Describe your product"></textarea>
                    <span asp-validation-for="Description" class="text-danger small"></span>
                </div>

            <div class="form-group full-width">
    <label asp-for="Category" class="form-label">Category</label>

    <select asp-for="Category" class="form-control custom-input">
        <option value="">-- Select Category --</option>
        <option value="Electronics">Electronics</option>
        <option value="Clothing">Clothing</option>
        <option value="Accessories">Accessories</option>
        <option value="Beauty & Personal Care">Beauty & Personal Care</option>
        <option value="Home & Living">Home & Living</option>
        <option value="Food & Beverages">Food & Beverages</option>
        <option value="Books & Stationery">Books & Stationery</option>
        <option value="Sports & Outdoors">Sports & Outdoors</option>
        <option value="Toys & Games">Toys & Games</option>
        <option value="Automotive">Automotive</option>
        <option value="Others">Others</option>
    </select>

    <span asp-validation-for="Category" class="text-danger small"></span>
</div>
                <div class="form-group">
                    <label asp-for="Price" class="form-label">Price (₱)</label>
                    <input asp-for="Price" class="form-control custom-input" type="number" step="0.01" placeholder="0.00" />
                    <span asp-validation-for="Price" class="text-danger small"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Stock" class="form-label">Stock</label>
                    <input asp-for="Stock" class="form-control custom-input" type="number" placeholder="0" />
                    <span asp-validation-for="Stock" class="text-danger small"></span>
                </div>

                <div class="form-group full-width">
                    <label asp-for="ProductImages" class="form-label">Product Images</label>
                    <input asp-for="ProductImages"
                           class="form-control custom-input"
                           type="file"
                           multiple
                           accept="image/*" />
                    <span asp-validation-for="ProductImages" class="text-danger small"></span>
                    <small class="text-muted">
                        You can upload multiple product images. Allowed: JPG, JPEG, PNG, WEBP.
                    </small>
                </div>
            </div>

            <div class="form-divider"></div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary save-btn">
                    Save Product
                </button>

                <a asp-controller="Seller" asp-action="Products" class="btn btn-outline-secondary cancel-btn">
                    Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}