@{
    Layout = "_DashboardLayout";
    ViewBag.ActiveRole = "Seller";
    ViewData["Title"] = "Seller Dashboard";

    var productCount = ViewBag.ProductCount ?? 0;
    var pendingOrders = ViewBag.PendingOrders ?? 0;
    var lowStock = ViewBag.LowStock ?? 0;
    var monthlySales = ViewBag.MonthlySales ?? 0;
}

<link rel="stylesheet" href="~/css/sellerdashboard.css" asp-append-version="true" />

<div class="seller-dashboard-page">

    <div class="seller-hero">
        <div>
            <span class="seller-badge">Seller Dashboard</span>
            <h1>Welcome back, @User.Identity?.Name!</h1>
            <p>Manage your products, track orders, and monitor your shop performance.</p>
        </div>

        <a asp-controller="Seller"
           asp-action="CreateProduct"
           class="hero-add-btn">
            + Add Product
        </a>
    </div>

    <div class="seller-stats-grid">
        <div class="seller-stat-card">
            <span>Total Products</span>
            <strong>@productCount</strong>
            <p>Active product listings</p>
        </div>

        <div class="seller-stat-card">
            <span>Pending Orders</span>
            <strong>@pendingOrders</strong>
            <p>Orders waiting for action</p>
        </div>

        <div class="seller-stat-card warning">
            <span>Low Stock</span>
            <strong>@lowStock</strong>
            <p>Products needing restock</p>
        </div>

        <div class="seller-stat-card">
            <span>Monthly Sales</span>
            <strong>₱@monthlySales</strong>
            <p>Total sales this month</p>
        </div>
    </div>

    <div class="seller-content-grid">

        <div class="seller-card">
            <div class="seller-card-header">
                <div>
                    <h2>Quick Actions</h2>
                    <p>Common tasks for managing your shop.</p>
                </div>
            </div>

            <div class="quick-action-grid">
                <a asp-controller="Seller" asp-action="Products" class="quick-action-card">
                    <span>📦</span>
                    <strong>Manage Products</strong>
                    <p>Edit, delete, and view your items.</p>
                </a>

                <a asp-controller="Seller" asp-action="CreateProduct" class="quick-action-card">
                    <span>➕</span>
                    <strong>Add Product</strong>
                    <p>Create a new product listing.</p>
                </a>

                <a href="#" class="quick-action-card">
                    <span>🛒</span>
                    <strong>Orders</strong>
                    <p>View and process buyer orders.</p>
                </a>

                <a href="#" class="quick-action-card">
                    <span>🏪</span>
                    <strong>Shop Profile</strong>
                    <p>Update your seller information.</p>
                </a>
            </div>
        </div>

        <div class="seller-card recent-card">
            <h2>Recent Activity</h2>

            <div class="activity-list">
                <div class="activity-item">
                    <span></span>
                    <div>
                        <strong>Your store is ready</strong>
                        <p>You can now manage your shop.</p>
                    </div>
                </div>

                <div class="activity-item">
                    <span></span>
                    <div>
                        <strong>Add products</strong>
                        <p>Start adding products for shoppers to browse.</p>
                    </div>
                </div>

                <div class="activity-item">
                    <span></span>
                    <div>
                        <strong>Monitor orders</strong>
                        <p>Check incoming orders regularly.</p>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>